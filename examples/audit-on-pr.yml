name: Tremor Audit

on:
  pull_request:

permissions:
  contents: read
  pull-requests: write

jobs:
  tremor-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: ENNA-tools/tremor@main
        with:
          mode: audit
          severity-threshold: medium
